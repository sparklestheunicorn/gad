export const styles = (theme) => ({
  rephrase: {
    label: 'rephrase',
    margin: `${theme.spacing.M} 0`,
  },
  terms: {
    label: 'terms',
    borderTop: `1px solid ${theme.color.border}`,
  },
  termName: {
    fontWeight: 'bold',
  },
})
